{
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter=./apps/** run dev",
    "dev:packages": "pnpm -r --parallel --filter=./packages/** run dev --watch",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.0.3"
  },
  "packageManager": "pnpm@8.6.10",
  "name": "pianocheat"
}
